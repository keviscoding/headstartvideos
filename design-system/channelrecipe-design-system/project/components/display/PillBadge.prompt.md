**PillBadge** — the rounded eyebrow pill that sits above a headline. Keep copy claim-free.

```jsx
<PillBadge>New: this week's recipe just dropped</PillBadge>
```

`dot` toggles the violet drop indicator; `theme="dark"` for the app.
