**Stepper** — the six-step pipeline indicator. Completed steps get a violet check, the current step a violet ring; the connecting line fills violet as you progress.

```jsx
<Stepper current={2} />
<Stepper steps={["Recipe","Title","Script","Voice","Thumbnail","Cook"]} current={0} theme="dark" />
```
